# Decisions

- [services] Python 3.14 is what is installed locally; code stays 3.11-compatible (no 3.12+ only syntax).
- [services] UI (M6/M7) deferred per phase change; Makefile has `test-js` instead of `test-frontend`, and `demo-cli` (headless) instead of a UI `demo`; no `test-e2e` yet.
- [services] Service-side test fixtures live in `contracts/examples/` (data/fixtures is intel's folder).
- [services] Single root `requirements.txt` for the whole backend (includes pandas/scikit-learn/joblib for intel).
- [services] Makefile uses `PY ?= python` so it works on Windows Git Bash and elsewhere (`make PY=python3`).
- [services] API imports intel functions only via `backend/api/intel.py`, which switches between stand-ins and real modules with `USE_STANDINS`.
- [services] Added GET /weather/today (morning briefing needs heat/rain) and POST /reset (fresh demo state) beyond the PLAN.md endpoint list.
- [services] Scenario contract is `{name, task_id, windows}` so the debrief knows which task the replay belongs to.
- [services] Debrief overrun vs CAT estimate = uncontrollable_min + controllable_min ("9 minutes over" = 6 + 3).
- [services] Logging an incident auto-writes a `memory_incident` MemoryNote for the machine (48 h TTL).
- [services] Voice queue: a safety line cancels a playing coaching/info line (dropped) or care line (replayed after); the budget is used up when a coaching line is accepted, not when quiet mode drops it.
- [services] Speaker renders the event itself so it can switch to the English text when no Hindi voice exists (sets `fallbackToEnglish`).
- [services] Replay idle = machine_active false OR idling_time_min >= 10 in the window; idle lesson plays once per idle stretch once it reaches 10 min.
- [services] demo-cli drives the API in-process with FastAPI TestClient on a temp DB (no server to start); in-task lines come from the real JS replay via scripts/replay_cli.mjs.
- [services] Makefile sets APPDATA via cygpath when MSYS make drops it (otherwise pip --user packages are invisible).
- [services] Contracts aligned to what intel built (PLAN.md left these open): weather is a list `{hour, temperature_c, rain, wind_kmh}`; `scenario()` returns a window list and the API wraps it as `{name, task_id, windows}` with task_id = first task of today's plan; message keys are dotted (`warn.*`, `finding.*`); break reason `regular|heat`; telemetry timestamps in CSV style.
- [services] `contracts/examples/demo_scenario.json` is exported from the real API by `make export-scenario`; the JS integration test replays it through replay -> rules -> queue.
- [services] Offline voice: `make audio` (scripts/generate_audio.py, edge-tts in requirements-dev.txt) pre-renders MP3s to frontend/public/audio/{lang}/{sha1(voice|mode|text)[:16]}.mp3 plus manifest.json; the app itself still makes no network calls.
- [services] Lines rendered = every say() of the headless demo (demo_cli run with say() intercepted, so text is identical) + every slot-free template + shift_hello per machine and memory_incident/incident_logged per category, in en and hi (88 clips, ~3 MB, committed).
- [services] Voices: en-IN-PrabhatNeural and hi-IN-MadhurNeural (male, matching the masculine Hindi templates, e.g. "बोलूँगा").
- [services] modes.js -> edge-tts: rate (r-1)*100 %, volume (v-1)*100 %, pitch (p-1)*50 Hz (alert = +15%/+0%/+15Hz, care = -15%/-20%/-5Hz); a test keeps the Python copy of MODES equal to modes.js.
- [services] speaker.js looks up message_key + lang + exact rendered text (preferring the same mode); a hit plays the MP3 (even with no Hindi OS voice, so no fallback warning); a miss, a load error or an autoplay block falls back to speechSynthesis. Interface unchanged; the result gains `source: audio|speech|none` and the speaker a `ready` promise (manifest fetch in the browser).
- [services] Offline voice: `make audio` (edge-tts, dev-only dependency in requirements-dev.txt) pre-renders lines to frontend/public/audio/{lang}/{hash}.mp3 with voices hi-IN-MadhurNeural and en-IN-PrabhatNeural (male, to match the masculine Hindi forms in the templates, e.g. "बोलूँगा").
- [services] Lines covered: everything said by the headless demo (demo_cli's rendering), the in-task lines from replaying the real contracts/examples/demo_scenario.json, and every slot-free template (safety, belt, breaks, lessons, commands). Lines with other slot values (e.g. a different proximity distance) use speechSynthesis.
- [services] modes.js -> edge-tts prosody: rate (r-1)*100 %, pitch (p-1)*100 Hz, volume (v-1)*100 % (alert = +15% / +30Hz / +0%). Volume is baked into the MP3.
- [services] Filename = sha1(voice|prosody|text)[:16]; unchanged lines are not re-downloaded, stale MP3s are pruned. Manifest entries = {message_key, lang, mode, text, slots, file}; lookup is by exact (message_key, lang, rendered text), preferring the same mode. A Vitest test checks JS render() reproduces every manifest text, so Python/JS rendering cannot drift silently.
- [services] speaker.js order: MP3 for the requested language -> (no Hindi TTS voice) English MP3 -> speechSynthesis. A Hindi MP3 plays even without a Hindi TTS voice (no fallback flag). Load/play errors (incl. autoplay blocking) fall back to speechSynthesis; a cancelled clip never fires end or fallback. The manifest is injected (`manifest` option or `setManifest(await loadManifest())`), so the speak/cancel interface is unchanged.
- [services] Audio was generated with intel stand-ins for morning/debrief numbers; rerun `make audio` after intel merges so those lines match the real predictions (fixed + in-task lines are already real).
- [services] Phrasing variants: every non-safety template key has 3 phrasings (the original + 2 alternatives) in en + hi; en[i] and hi[i] say the same thing with the same slots, so the English fallback keeps the chosen phrasing. Safety keys (SAFETY_KEYS: belt_before_move, seatbelt_unfastened, proximity_alert, safety_alert, warn.*, memory_incident) keep exactly one fixed phrasing; tests enforce both rules.
- [services] Variant choice lives in the voice queue (`createQueue({ phraser })`): a line gets `variant` when accepted, unless the event already has one; repeat() replays the same words. `createPhraser({ seed: DEMO_SEED })` (mulberry32, seed 42) for demo mode, `createPhraser()` (Math.random) otherwise; a key never gets the same phrasing twice in a row. SaathiEvent gains an optional `variant` field.
- [services] demo_cli picks variants with a seeded Python PRNG (deterministic run to run, but not the same sequence as the browser); `make audio` pre-generates every phrasing of every demo/fixed line (196 clips, ~7.4 MB), so whichever variant the app picks has an MP3.
- [services] Phraser gains `note(key, variant)`: when an event already carries a `variant`, the queue records it via `note()` (same memory as `pick()`), so the next auto-picked line for that key never repeats it. `note` is optional on custom phrasers (`phraser.note?.()`).
- [ui] Frontend = React 18 + Vite 5 + react-router 6 + framer-motion + lucide-react; fonts bundled via @fontsource (offline). App state is one tiny store (`src/state/store.js`, useSyncExternalStore), no state library.
- [ui] The operator app is mounted once; Stage View (/stage/...) only wraps it in a 1280x800 tablet frame scaled to fit plus the "Under the hood" panel, so S toggles views without restarting a screen, replay or voice line.
- [ui] Voice runtime (`src/saathi/voiceRuntime.js`) wraps the engine's speaker to drive captions, avatar state, the safety banner and the Stage View queue log (spoken / held-budget / held-quiet / preempted, phrasing variant, MP3 vs browser voice). Engine files in src/voice and src/replay are unchanged.
- [ui] Briefings are spoken in order with `sayInOrder` (each line waits for the previous one) so a safety line inside a briefing (condition warning, memory note) never preempts the line before it.
- [ui] Screen lines mirror scripts/demo_cli.py (`src/saathi/briefings.js`); each briefing is spoken once per shift (revisiting a screen stays quiet).
- [ui] In-task progress bar = replay progress (windows played / total) mapped onto Saathi's predicted minutes; the scenario's 15-min windows are not task minutes.
- [ui] Demo pace: replay at 180x (one window every 5 s) in Normal; Max pace (e2e) replays at 6000x and plays each line silently for 120 ms (the speaker still looks up MP3 vs TTS so the queue log is real). The In-task control keeps 1x/10x/60x.
- [ui] Demo step 7 checks fatigue drift only when /health reports real intel modules (standins=false); with stand-ins it shows the planned break (break_time) and says so in the Stage View log, avoiding a 404 for the missing "fatigue" scenario.
- [ui] Captions sit bottom-left, clear of the bottom-right column where every screen keeps its primary action; on Morning they sit over the hero so "Last shift says…" stays visible; on In-task, safety lines show as the red banner and lessons as the lesson card instead of a caption.
- [ui] Recommended lessons map findings -> lessons (idling -> engine off, fuel -> smooth cycles, unbelted/alerts -> walk-around, fatigue -> hydration) then conditions (rain+trenching -> trench edge, heat -> hydration). Hub lessons play as `info` priority (operator asked), so the one-coaching-line budget only limits unprompted lessons.
- [ui] What-if needs a prediction for an edited task, which the API does not offer (GET /predict/{task_id} only). The panel calls POST /predict (body = Task) when the API advertises it; reported to services rather than changing backend/.
- [ui] Playwright specs live in e2e/ but Playwright is installed in frontend/; frontend/playwright.config.js sets NODE_PATH so the specs resolve it. Run: `cd frontend && npx playwright test` (starts the API and Vite if not running).
- [ui] What-if checks /openapi.json for POST /predict before calling it (no 404s in the console); until services adds it the panel shows its controls disabled with a note. Pre-task already renders a what-if prediction (store.whatIf) and the new estimate is spoken (browser voice when no MP3 matches).
- [ui] Architecture: full SVG diagram at /architecture; the Stage View panel shows the same six stages as a compact chip strip (the SVG was unreadable at panel width). The active stage is set by the API wrapper, replay runtime and voice runtime.
- [ui] Voice incident logging: webkitSpeechRecognition (en-IN / hi-IN) -> POST /incidents/classify -> confirm dialog (category can be changed) -> source "voice", transcript as note. Fixed commands work in the same box (repeat, quiet mode, taking a break; "log incident" alone asks what happened). Without speech recognition, or on error, the operator types instead.
- [services] Intel merged: removed backend/api/standins.py and USE_STANDINS; intel.py imports data_gen/ml/planner directly. /health keeps `standins: false` because frontend/src/api.js reads it.
- [services] Fixed merge leftovers: test_generate_audio.py rewritten for the current generate_audio.py API (prosody/file_for/collect_lines, manifest entries with slots+variant); voice.test.js expects speaker source 'tts' (what speaker.js and the UI use).
- [ui] Top-bar mute is "Coaching mute" (store.quiet): it holds every non-safety line (coaching, care, info), cuts a playing non-safety line at once and skips queued ones; safety always speaks. Done in src/saathi/voiceRuntime.js; the src/voice queue is unchanged (it still drops coaching only).
- [ui] No playlists: a screen opens with one headline line (greeting / pre-task estimate / debrief result) plus its safety lines (`triggerLines`); the rest stays on screen. Lines in sequence get a 0.9 s pause. Demo step 7 speaks only the fatigue finding line.
- [ui] Saathi chip idle state is "Ready"; "Listening…" shows only while speech recognition is running (store.listening). Demo step shows as a badge in the operator top bar.
- [ui] Replay speeds 1×/10×/60× are multiples of the demo pace (1× = one 15-min window every 5 s, engine speed 180); the store keeps the multiple, demo max pace = 60×. In-task pause pauses the whole demo while it runs; the speed control sits above the safety banner so it stays clickable during an alert.
- [services] UI audio unlock: the "Start Saathi" overlay shows once per browser-tab session (flag in sessionStorage); lines said before the click wait and play after it (they used to fire behind the overlay, get blocked and "finish" instantly). A line the browser still blocks (e.g. after a reload) is retried silently on the next tap/key press; the overlay never returns.
- [services] Coaching mute (top-bar button): safety lines stay spoken; everything else stops at once (including the current line) and is shown as a caption only. Button reads "Coaching mute" / "Coaching muted" with a tooltip.
- [services] Status chip: "Saathi ready" when idle; "Listening…" only while speech recognition is actually running (VoiceLog mic sets store.recognizing).
- [services] Screens speak one point per trigger: safety lines + the screen's headline (Morning: greeting; Pre-task: estimate; Debrief: overrun split + "not in your hands"); task cards, rain/heat, order, breaks and findings stay on screen only. Demo mode: "Step N of 7" badge in the top bar, 2.5 s pause (badge shows "Next: …") after each step's last line. Pre-task's primary button is "Begin work" (काम चालू करें); its back arrow has a label/tooltip.
- [services] Merge ui-fixes -> backend (both branches fixed the same UI bugs in parallel). Kept from backend: coaching-mute `setQuiet` (cuts via finish() so the queue moves on, logs 'muted', `{cut:false}` lets a spoken "quiet mode on" confirmation finish) instead of ui-fixes' dropPending/clear version, which would cut that confirmation; `triggerLines` in useBriefing (one headline line + safety, applied centrally) instead of ui-fixes' per-screen `spokenLines` (so the debrief no longer adds "not in your hands" after the split); store.listening + "Ready"/"Muted — safety only" chip labels instead of store.recognizing; their mute button, tooltips and i18n wording where both defined a key.
- [services] Kept from ui-fixes (backend had no equivalent): session-persistent audio unlock (Start overlay once per tab session), lines held until the click, silent retry of blocked lines on the next gesture, caption-only display of muted lines (merged into backend's single `muted()` check in say(); the auto-merge had left two mute checks), 2.5 s pause between demo steps. Combined: demo badge reads "Step N of 7" with backend's icon and shows "Next: …" during the pause; Pre-task button reads "Begin work: <task>" (ui-fixes' rename + backend's task name and tooltip).
- [services] `belt_before_move` fires only when the idle stretch before an unbelted resume was >= `BELT_MIN_IDLE_MIN` = 2 idle minutes (rules.js, tunable via `beltMinIdleMin`), so brief pauses do not wear out the alert; after a shorter pause an unbelted resume speaks the ordinary `seatbelt_unfastened` line instead of staying silent. Demo unchanged (fires at 09:15 after a 24-min stop).
- [intel] Fatigue vs harder work: tried adding a load-cycles trend (cycles / shift's earlier median) as a 5th IsolationForest feature — rejected: it still flagged costly-terrain shifts (11/28 vs 12/28 at >= 1.0 L/cycle; a forest flags whatever dimension is unusual) and worsened synthetic results (11/21 caught vs 12/21, mid-shift false alarms 4/1764 vs 1/1764). Forest features unchanged.
- [intel] Instead the cycle trend gates the forest: a sustained last-hour anomaly counts as fatigue_drift only if last-hour median load cycles <= 0.8 x the shift's earlier median (`THROUGHPUT_DROP`) or >= 2 unbelted/alert windows (`GATE_MIN_LAPSES`). Measured on the 84 synthetic shifts (`python -m backend.ml.eval_drift`): 12/21 drift shifts caught, 0/63 full-shift false positives, 1/1764 (0.06%) mid-shift false alarms (unchanged), demo fires only at 13:45, crafted drift fires, 0/56 harder-terrain shifts flagged (was 24/56 without the gate); threshold insensitive over 0.75-0.9.
- [intel] Debrief wording: `backend.ml.debrief_lines()` is the one place that picks debrief lines. Below `MIN_ATTRIBUTION_OVERRUN_MIN` = 3 min total overrun it says only the number (`debrief_near_time`: "2 minutes over. Close to plan.") with no attribution and no "not your fault": the estimator's own error is a few minutes, so explaining a 1-2 min overrun claims more confidence than the model has and sounds hollow. From 3 min: `debrief_over` + `debrief_not_your_fault` when uncontrollable >= controllable (unchanged). `demo_cli` uses it; demo (9 min) unchanged.
- [services] Voice commands: every phrase must have >= `MIN_PHRASE_WORDS` = 2 words (enforced in the matcher, not just the lists) so single common words overheard in the cab ("quiet", "break", "again", "आराम", "चुप") never trigger; bare-word triggers were replaced by short phrases ("quiet mode", "taking a break", "repeat that", "चुप रहो", "आराम करना"). `matchCommand(text, {confidence})` also rejects recogniser confidence < `MIN_COMMAND_CONFIDENCE` = 0.6; confidence missing or 0 (some browsers always report 0) = unknown, phrase rule only. Trade-off: fewer false triggers, slightly less forgiving of casual one-word phrasing.
- [services] Machine Memory expiry: known simplification — full context-aware expiry is future work; only weather notes get a shorter window for the hackathon. Notes whose message_key is a weather warning (`warn.rain_slippery`, `warn.rain_trench_edge`, `warn.wind_caution`, `warn.heat_hydration`) expire after `WEATHER_NOTE_TTL_HOURS` = 12 h via `config.note_ttl_hours()`; everything else (incl. all incident notes) keeps 48 h.
- [services] Briefing guard (`frontend/src/voice/briefing.js`): every task-facing screen awaits `guard.ensureBriefed(machine_id)` on load; Machine Memory notes not yet spoken this session are queued at safety/alert priority before the screen's own lines, so skipping or reopening past the morning screen never hides them. "Session" = guard instance (app reopen repeats notes once, the safe side); the morning screen can `markHeard()` notes it spoke; API down resolves to no notes and never blocks the task flow.
- [services] `debrief_near_time` (1 and 2 min) added to the pre-rendered audio lines.
- [intel] Cold start, drift: new operators get no drift detection until they have ~2h of history; this is expected, not a bug. `fatigue_drift()` skips any operator group with fewer than `MIN_HISTORY` = 8 windows (deliberate since P1). The count is of the windows being analysed, so a first-shift operator is covered after ~2 h of that shift; the forest itself is trained on pooled data, never on one operator's past.
- [intel] Cold start, wording: `operator_history(operator_id)` -> `{operator_id, shift_count, history_available}` counts the operator's past shifts (runs of 15-min windows per machine) in the on-device telemetry (synthetic: 28 each for OP1001-3). With `history_available` false, `debrief_lines()` uses `debrief_over_first_shift` ("N minutes over the CAT estimate...") and `finding_lines()` swaps `finding.fatigue_drift` ("Your rhythm is slowing down") for `finding.fatigue_drift_first_shift` ("The pace has slowed compared with earlier in this shift"). Only these two lines have variants; no other line claims a personal comparison. Note: `debrief_over` never compared against personal history (it compares with the CAT estimate and a pooled model), so its variant only makes that anchor explicit.
- [services] `GET /operators/{operator_id}/history` exposes the count via intel.py (stand-in: OP1001-3 = 28 shifts, others 0); read-only, nothing stored. `demo_cli` picks debrief and finding lines with it; the demo operator has history so the demo is unchanged. `finding.fatigue_drift_first_shift` added to the pre-rendered audio.
- [intel] Drift line made universal (supersedes the drift half of the cold-start entry above): `finding.fatigue_drift` now says "The pace has slowed compared with earlier in this shift" for every operator, and `finding_lines()` no longer takes history, because the detector's comparison is always intra-shift, never cross-shift; `finding.fatigue_drift_first_shift` is retired. Only `debrief_over` / `debrief_over_first_shift` still branch on history.
- [services] Merge main -> backend: kept backend/api/standins.py deleted (backend removed stand-ins in aa304e5 and nothing references them; offline dev is covered by the frontend's own fixture mode). main's only stand-in change was an `operator_history` stand-in; the real `operator_history` is wired in intel.py directly. Note: main still defaulted to stand-ins; the "real modules by default" change lives on the unmerged demo-hardening branch, not main.
- [services] Merge main -> backend, templates: backend requires 3 phrasings per non-safety key, main added single-string lines. `finding.fatigue_drift` keeps main's universal intra-shift wording as phrasing 0, with two more intra-shift phrasings replacing backend's personal ones ("Your rhythm is slowing down"); `debrief_over_first_shift` (all anchored to the CAT estimate) and `debrief_near_time` (number only) got two more phrasings each. New Hindi phrasings need native review like the rest of the file.
- [services] Merge main -> backend, audio: backend's generate_audio.py (variants, prosody read from modes.js via the template export) kept; main's FIXED_SLOT_LINES (greeting per machine, memory note / incident confirmation per category, near-time debrief 1-2 min) added to collect_lines(); `memory_incident` renders in alert mode, `finding.fatigue_drift` fixed clip in care mode. main's MODES/PITCH_HZ_PER_UNIT constants dropped (unused; backend maps pitch as (p-1)*100 Hz). demo_cli: main's finding_lines/debrief_lines/operator-history calls kept, printed through backend's out() so the silent audio run stays silent.
